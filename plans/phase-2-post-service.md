# Phase 2: Post Service & Content Management

**Goal:** Create the service responsible for handling posts.

## Tasks

- [ ] Set up PostgreSQL container for Post DB (Database-per-service pattern).
- [ ] Initialize **Post Service** (Node.js/Express).
- [ ] Implement CRUD operations for Posts.
- [ ] Implement JWT Validation middleware (Shared or duplicated logic).
- [ ] Link Posts to Users (store `userId` in posts).

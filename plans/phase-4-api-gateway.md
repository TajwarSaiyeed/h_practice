# Phase 4: API Gateway & Reverse Proxy

**Goal:** Unified entry point for the frontend/client.

## Tasks

- [ ] Set up **Nginx** as an API Gateway.
- [ ] Configure routing:
  - `/api/users` -> User Service
  - `/api/posts` -> Post Service
  - `/api/interactions` -> Interaction Service
- [ ] Implement basic rate limiting and security headers in Nginx.
